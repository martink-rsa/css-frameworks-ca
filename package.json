{
  "name": "css-frameworks-ca",
  "version": "0.0.1",
  "description": "This is a Tailwind-based CSS Frameworks course assignment.",
  "main": "index.js",
  "scripts": {
    "build": "npx tailwindcss -i ./css/input.css -o ./css/style.css --minify",
    "dev": "npx tailwindcss -i ./css/input.css -o ./css/styles.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "css frameworks",
    "tailwind"
  ],
  "author": "Martin Kruger",
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  }
}
